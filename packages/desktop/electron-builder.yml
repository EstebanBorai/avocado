appId: com.example.app
productName: Avocado
artifactName: "${os}-${productName}-${version}.${ext}"

directories:
  output: "./build/desktop"
files:
  - "./app/**"
  - "./build/ui/**"

# TODO: Define specific icons when ready
# mac:
#   target:
#     - default
#   icon: build/icon.png
# win:
#   target:
#     - nsis
#   icon: build/icon.png
# linux:
#   target:
#     - deb
#     - AppImage
#   category: Network
